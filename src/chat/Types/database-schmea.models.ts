export enum ConversationType {
  SINGLE = 'single',
  GROUP = 'group',
}

export enum FileTypeEnum {
  IMAGE = 'image',
  VIDEO = 'video',
  AUDIO = 'audio',
  DOCUMENT = 'document',
  OTHER = 'other',
}

export enum MessageType {
  TEXT = 'text',
  FILE = 'file',
}